package com.improvemyskills.evolution.tp;

//import java.lang.classfile.ClassFile;import java.lang.classfile.Class.file.Path;

public class ReadClass {

    public static void main(String[] args) throws Exception {

        /**
         * Read a .class
         */
/*        ClassFile cf = ClassFile.of();

        var model = cf.parse(Path.of("MyClass.class"));

        model.methods().forEach(method ->
                System.out.println("Method: " + method.methodName())
        );*/

        /**
         * Build a bytecode
         */
/*        byte[] bytes = cf.build(
                cf.classBuilder("MyGeneratedClass")
                        .build()
        );*/
    }
}
